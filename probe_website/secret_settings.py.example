# These credentials are used for dataporten, and is taken from the
# dataporten dashboard for your app. Leave unchanged if
# dataporten/feide is not to be used
OAUTH_CREDENTIALS = {
        'id': 'ClientID',
        'secret': 'ClientSecret'
}

# Key used for cookie signing. Can be generated by doing
# >>> import os
# >>> os.urandom(32)
# in an interactive python shell, and copying the resulting binary string
# directly
SECRET_KEY = 'signing key'
